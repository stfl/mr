[$HOME/.config/vcsh/repo.d/<repo>.git]
checkout =
   vcsh clone git@bitbucket.org:stefanlendl/<repo>.git || vcsh <repo> checkout master --force
update   =
   vcsh <repo> pull --rebase
push     = vcsh <repo> push
status   = vcsh <repo> status --short
gc       = vcsh <repo> gc
