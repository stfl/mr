[$HOME/.config/vcsh/repo.d/mr.git]
checkout =
  vcsh clone git@bitbucket.org:stefanlendl/mr.git
update   =
  vcsh mr pull --rebase
# to set the relevant config afterwards
push     = vcsh mr push
status   = vcsh mr status --short
gc       = vcsh mr gc
